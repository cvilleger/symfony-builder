services:
  - docker

before_install:
  - docker build .

script:
  - docker --version

deploy:
  provider: releases
  api_key:
    secure: YOUR_API_KEY_ENCRYPTED
  file: "build.zip"
  overwrite: true
  skip_cleanup: true
  draft: true
